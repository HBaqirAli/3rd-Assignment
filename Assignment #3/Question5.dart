// Create a map with name, phone keys and store some values to it. Use where to find all keys that have length 4?
void main() {
  Map data = {
    "Name": "Baqir Ali",
    "MY": "Pak",
    "Contact": "Num",
    "is": "03138521820",
  };
  var key = data.keys.length;
  print(key);
}
